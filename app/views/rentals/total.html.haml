%h1= t('total_rentals')

%table
  %thead
    %tr
      %th= rental_t('serial')
      %th= rental_t('box')
      %th= rental_t('customer_name')
      %th= rental_t('is_active')
      %th

  %tbody
    = render @rentals
